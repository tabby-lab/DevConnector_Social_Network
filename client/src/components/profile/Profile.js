import React from 'react';
import PropTypes from 'prop-types';
import { connect } from 'react-redux';
import Spinner from '../layout/Spinner';
import {getProdileById} from '../../actions/profile';

const Profile = props => {
    return (
        <div>
            
        </div>
    )
}

Profile.propTypes = {

}

export default Profile
